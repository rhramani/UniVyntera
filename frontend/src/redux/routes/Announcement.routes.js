import { BASEURL } from "../../baseUrl";

export const announcementsAddUrl = `${BASEURL}/announcement/sendMail`;
export const announcementsUploadUrl = `${BASEURL}/announcement/upload`;
export const announcementsGetUrl = `${BASEURL}/announcement/getHistory`;
export const announcementsDeleteUrl = `${BASEURL}/announcement/delete`;